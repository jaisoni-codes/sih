# VOICE / REGIONAL LANGUAGE
IndicVoices includes Indian-language speech resources including Santali, but access is gated.
Use ASR to convert voice -> text, then send text to `/api/ai/process-complaint`.
Do not put ASR credentials/tokens in the frontend.
